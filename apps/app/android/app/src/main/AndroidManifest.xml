<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Notifications (OPH-061/063, docs/NOTIFICATIONS.md §1): runtime
         notification permission (13+), exact alarms behind the Android 14
         special-access flow, reboot rescheduling, and the full-screen alarm
         UX (gated + Play-declared; the OS degrades gracefully). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- OPH-304, ADR-0037. Measured on an API 36 emulator before this line was
         written: SCHEDULE_EXACT_ALARM is `signature|privileged|appop`, and the
         list of packages holding it was EMPTY for us. We target SDK 36, so on
         Android 14+ every install starts WITHOUT exact alarms until the user
         finds "Alarms & reminders" in system settings by hand — which is not a
         thing a person does for an alarm they have not yet missed.

         USE_EXACT_ALARM is `normal`: granted at install, not revocable without
         uninstalling. It is also a Play RESTRICTED permission — declaration
         form plus a video of the core feature, and a manual review that can
         refuse. SCHEDULE_EXACT_ALARM stays beside it on purpose: it is what
         Android 12 (minSdk 24 reaches back much further) uses, and the two are
         complementary rather than alternatives. -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT"/>
    <!-- OPH-223: on-device speech-to-text for voice task capture. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <!-- OPH-244 (DESIGN §30 A3): AllisWell asks for NO media permission, ever.
         Photos come from the Android Photo Picker and the camera from
         ACTION_IMAGE_CAPTURE, both of which hand back only what the user chose
         and need nothing declared. These lines strip the permission if any
         dependency ever merges one in — Play's photo/video policy rejects apps
         that declare READ_MEDIA_* when the Photo Picker would do, and
         image_picker only takes its runtime-permission branch when CAMERA is
         present in the merged manifest.
         `tools:node="remove"` is SILENT when nothing matches, which is why
         scripts/android/assert-permissions.sh checks the built APK too. -->
    <uses-permission android:name="android.permission.CAMERA" tools:node="remove"/>
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" tools:node="remove"/>
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" tools:node="remove"/>
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" tools:node="remove"/>
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" tools:node="remove"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" tools:node="remove"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" tools:node="remove"/>
    <application
        android:label="AllisWell"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- OPH-189 (ADR-0016): alliswell:// deep links — the widget's tap,
                 calendar events, note embeds. A custom scheme, so no
                 android:autoVerify (that is for App Links over https). -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="alliswell"/>
            </intent-filter>
            <!-- OPH-225 (ADR-0023): the share target — "Share to AllisWell"
                 receives text and URLs (receive_sharing_intent). v1 is text
                 only; images/files are ignored downstream. -->
            <intent-filter>
                <action android:name="android.intent.action.SEND"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="text/plain"/>
                <data android:mimeType="text/html"/>
            </intent-filter>
            <!-- Round 16 follow-up: AllisWell opens markdown files. VIEW is
                 the "Open with" path (a file manager, a mail attachment) and
                 SEND is the share sheet. Both mime types AND the extension
                 patterns are declared: Android routinely reports a .md file as
                 text/plain or application/octet-stream depending on where it
                 came from, so a mime-only filter misses most real files. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:mimeType="text/markdown"/>
                <data android:mimeType="text/x-markdown"/>
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <!-- ADR-0030: EDIT is what a file manager sends when the user
                     means "open this so I can change it". Without it the app
                     is only ever offered as a viewer, which is the wrong
                     contract now that saving back exists. -->
                <action android:name="android.intent.action.EDIT"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:scheme="file"/>
                <data android:scheme="content"/>
                <data android:mimeType="*/*"/>
                <data android:host="*"/>
                <data android:pathPattern=".*\\.md"/>
                <data android:pathPattern=".*\\.markdown"/>
                <data android:pathPattern=".*\\..*\\.md"/>
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="text/markdown"/>
                <data android:mimeType="text/x-markdown"/>
            </intent-filter>
        </activity>
        <!-- flutter_local_notifications: exact-alarm delivery + reboot
             rescheduling (docs/NOTIFICATIONS.md §1). -->
        <receiver android:exported="false" android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />
        <receiver android:exported="false" android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
        <!-- OPH-133: home-screen tasks widget. The receiver renders the snapshot
             the app publishes via home_widget; the service backs its scrollable
             list. Interactivity (background complete/add) is OPH-132/133 follow-up. -->
        <receiver
            android:name=".TasksWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/tasks_widget_info" />
        </receiver>
        <!-- OPH-336: which list a placed widget shows. Exported because the
             launcher starts it (it accepts only this app's widget ids); its own
             empty task affinity so finishing it returns to the home screen, not
             into the app. -->
        <activity
            android:name=".TasksWidgetConfigureActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:taskAffinity=""
            android:label="@string/aw_widget_choose_list"
            android:theme="@style/AwWidgetConfigTheme">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>
        <service
            android:name=".TasksWidgetService"
            android:permission="android.permission.BIND_REMOTEVIEWS"
            android:exported="false" />
        <!-- OPH-341: home_widget's background receiver. Every Dart turn the
             native side asks for — the widget circle's "complete" (OPH-188),
             the six-hourly alarm refresh (OPH-321), the midnight redraw
             (OPH-334) — is an EXPLICIT broadcast to this class
             (HomeWidgetBackgroundIntent), and the plugin's own manifest is
             empty. Undeclared, all three went nowhere, silently, from the day
             they shipped. Not exported: every sender is this app's own process
             (a PendingIntent runs as its creator), and an exported receiver
             would let any app complete a task with alliswell://complete?id=…
             — the write-by-URL ADR-0016 forbids. -->
        <receiver
            android:name="es.antonborri.home_widget.HomeWidgetBackgroundReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="es.antonborri.home_widget.action.BACKGROUND" />
            </intent-filter>
        </receiver>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
